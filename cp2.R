# R的数据对象
## 存储角度：数值型（整数型、实数型） 字符型 逻辑型（TRUE FALSE）
## 结构角度：向量（Vector） 矩阵（Matrix） 数组（Array） 数据框（DataFrame） 列表（List）
## 创建——赋值 对象名 <- R数据对象
## 访问 print(对象名)
## 查看结构 str(对象名)
## 查看工作空间包含哪些对象 ls()
## 删除当前工作空间中的指定对象 rm(对象名) 或 remove(对象名)
